@model List<ContactoModel>;
@{
    ViewData["Title"] = "Listar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="card col-8">
    <div class="card-header ">
        <h1>Listar</h1>
    </div>
    <div class="card-body ">
        <a asp-action="Guardar" asp-controller="Contacto">Crea Nuevo Contacto</a>
            <table class="table table-striped">
                <thead>
                    <tr>
                     <th>Nombre</th>
                     <th>Telefono</th>
                     <th>Correo</th>
                     <th>Clave</th>
                     <th></th>
                 </tr>
                 </thead>
                 <tbody>
                    @foreach(var item in Model)
                    {
                        <tr>
                              <td>@item.Nombre</td>
                        <td>@item.Telefono</td>
                        <td>@item.Correo</td>
                        <td>@item.Clave</td>
                        <td>
                            <a asp-action="Editar" asp-controller="Contacto" asp-route-IdContacto="@item.IdContacto" class="btn btn-warning">Editar</a>
                            <a asp-action="Eliminar" asp-controller="Contacto" asp-route-IdContacto="@item.IdContacto" class="btn btn-danger">Eliminar</a>
                        </td>
                        </tr>
                      
                    }
                 </tbody>
                
           </table>
    </div>
</div>
